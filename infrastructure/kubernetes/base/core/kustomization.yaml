apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base Kubernetes manifests for core simulation services
# Services: simulation, stream-processor, frontend, kafka, redis, osrm

resources: []
# Manifests will be added in Phase 6 (Kubernetes Migration)
# - simulation-deployment.yaml
# - stream-processor-deployment.yaml
# - frontend-deployment.yaml
# - kafka-statefulset.yaml
# - redis-deployment.yaml
# - osrm-deployment.yaml

commonLabels:
  app.kubernetes.io/part-of: rideshare-simulation
  tier: core

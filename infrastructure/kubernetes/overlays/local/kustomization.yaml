apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local overlay for Kind cluster
# Small resource limits, local storage, development settings

resources:
  - ../../base/core
  - ../../base/data-platform

namespace: rideshare-local

# Overlays will be added in Phase 6:
# - Resource limits (small for local development)
# - Local storage class
# - NodePort services
# - Development environment variables
# - Single replica counts

# API base URL (REST endpoints)
VITE_API_URL=http://localhost:8000

# WebSocket URL (real-time updates)
VITE_WS_URL=ws://localhost:8000/ws

# Frontend logging level
VITE_LOG_LEVEL=info

# Note: Use .env.development for local development and .env.production for cloud builds
# Vite automatically loads .env.{mode} based on --mode flag:
#   npm run dev    → loads .env.development
#   npm run build  → loads .env.production
#
# Override locally with .env.local (not checked into git)

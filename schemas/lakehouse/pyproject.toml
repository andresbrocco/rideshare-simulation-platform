[project]
name = "lakehouse-schemas"
version = "0.1.0"
description = "PySpark schemas for lakehouse Bronze layer"
requires-python = ">=3.11"

[tool.mypy]
python_version = "3.13"
warn_unused_configs = true
check_untyped_defs = true
warn_redundant_casts = true
exclude = ["tests/"]

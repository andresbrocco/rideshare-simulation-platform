[project]
name = "dbt-rideshare"
version = "0.1.0"
description = "DBT project for rideshare data transformations"
requires-python = ">=3.11"

[tool.mypy]
python_version = "3.12"
warn_unused_configs = true
check_untyped_defs = true
warn_redundant_casts = true
mypy_path = "stubs"
